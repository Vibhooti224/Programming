Input : arr[ ] = {1, 2, 3, 4}
Output : 21
Explanation:
Sum up an array from index 0 to 1 = 3
Sum up an array from index 2 to 3 = 7
Their multiplication is 21.


CODE:
def multiply (arr, n) : 
    sum1=0
    sum2=0
    x=n//2
    for i in range(0,x):
        sum1=sum1+arr[i]
        
    for i in range(x,n):
        sum2=sum2+arr[i]
    return(sum1*sum2)
